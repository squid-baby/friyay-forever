// Template for secrets.h (which is gitignored). Copy to secrets.h and fill in.
// See PLAN.md §1.2 for policy — existing secrets stay inline in main.cpp;
// this landing spot is for NEW secrets going forward.
#pragma once

// Example shape for a future API key secret:
// #define SOMESERVICE_API_KEY "YOUR_KEY_HERE"
